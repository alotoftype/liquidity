
export default (props) => (
    <div className="staff">
        <img src={props.url}/>
        <p className="title">{props.title}</p>
        <p className="name">{props.name}</p>
        
    </div>    
)